DATABASE OPERATION IS  await operation

        #  const {hashedToken,unhashedToken ,tokenExpiry} = user.generateTemporaryToken; 
     this will give error because we are not  calling geeerate function

#      In Mongoose middleware:

# ALWAYS use function () {}

# NEVER use arrow () => {}